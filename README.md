 # seashine
This is my project.
<br>
Auther -Shanii
<br>
learing path is start 
for git bash = ls -a
for powershell = get-childitem -force
in vs ctrl+ shift+p = select default profile (to change the terminal in use)